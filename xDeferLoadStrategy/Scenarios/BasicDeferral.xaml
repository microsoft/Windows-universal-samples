<Page
    x:Class="xDeferLoadStrategy.BasicDeferral"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:xDeferLoadStrategy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <Grid x:Name="DeferredGrid" x:DeferLoadStrategy="Lazy">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Rectangle Height="100" Width="100" Fill="#F65314" Margin="12" />
            <Rectangle Height="100" Width="100" Fill="#7CBB00" Grid.Column="1" />
            <Rectangle Height="100" Width="100" Fill="#00A1F1" Grid.Row="1" />
            <Rectangle Height="100" Width="100" Fill="#FFBB00" Grid.Row="1" Grid.Column="1" />
        </Grid>
        <Button x:Name="RealizeElements" Content="Realize Elements" Click="RealizeElements_Click" Margin="12" />
    </StackPanel>
</Page>
