<Page
    x:Class="SDKTemplate.Scenario4_CompactOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="Description:" Style="{StaticResource SampleHeaderTextStyle}"/>
            <TextBlock Style="{StaticResource ScenarioDescriptionTextStyle}" TextWrapping="Wrap">
                Creates and shows am AppWindow as a Compact Overlay window (aka Picture-in-Picture).
            </TextBlock>
            <Button Name="showWindowBtn" Content="Create and show a CompactOverlay window" Margin="0,10,0,0" Click="ShowWindowBtn_Click"/>
        </StackPanel>
    </ScrollViewer>
</Page>
