<!--
//*********************************************************
//
// Copyright (c) Microsoft. All rights reserved.
// THIS CODE IS PROVIDED *AS IS* WITHOUT WARRANTY OF
// ANY KIND, EITHER EXPRESS OR IMPLIED, INCLUDING ANY
// IMPLIED WARRANTIES OF FITNESS FOR A PARTICULAR
// PURPOSE, MERCHANTABILITY, OR NON-INFRINGEMENT.
//
//*********************************************************
-->
<Page x:Class="AppUIBasics.NavigationRootPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:AppUIBasics"
        xmlns:data="using:AppUIBasics.Data" xmlns:navigation="using:AppUIBasics.Navigation"
        xmlns:controls="using:Windows.UI.Xaml.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <Style TargetType="Button" x:Key="HeaderButton">
                <Setter Property="Padding" Value="4,2,4,4" />
                <Setter Property="FontSize" Value="10" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="32" />
                <Setter Property="Width" Value="48" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Rectangle Fill="{ThemeResource NavigationViewDefaultPaneBackground}" x:Name="XboxContentSafeRect" Width="48" Visibility="Collapsed" />
        <controls:NavigationView x:Name="NavigationViewControl" AlwaysShowHeader="True" Header=" " Grid.Column="1" IsTabStop="False"
                SelectionChanged="NavigationView_SelectionChanged" Loaded="NavigationViewControl_Loaded">
            <controls:NavigationView.HeaderTemplate>
                <DataTemplate>
                    <local:PageHeader Title="{Binding}"
                        BackgroundBrush="{Binding Content, ElementName=rootFrame, Converter={StaticResource headerBackgroundConverter}}"
                        ShowAcrylicBehindHeader="{Binding Content, ElementName=rootFrame, Converter={StaticResource showAcrylicBehindHeaderConverter}}"
                                      />
                </DataTemplate>
            </controls:NavigationView.HeaderTemplate>
            <Frame x:Name="rootFrame" Navigated="rootFrame_Navigated"/>
        </controls:NavigationView>
        <Grid x:Name="CustomTitleGrid" VerticalAlignment="Top" Background="Transparent" Height="32" Loaded="BackButtonGrid_Loaded" Grid.ColumnSpan="2"/>
        <Grid x:Name="BackButtonGrid"  HorizontalAlignment="Left" VerticalAlignment="Top" Background="Transparent" Grid.Column="1">
            <Button x:Name="CustomBackButton" Style="{StaticResource HeaderButton}" Content="&#xE830;" FontSize="12" Click="Button_Click"/>
        </Grid>

    </Grid>
</Page>
