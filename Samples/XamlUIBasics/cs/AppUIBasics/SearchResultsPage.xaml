<Page x:Name="pageRoot" x:Class="AppUIBasics.SearchResultsPage" NavigationCacheMode="Enabled"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:AppUIBasics"
      xmlns:common="using:AppUIBasics.Common" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:data="using:AppUIBasics.Data" mc:Ignorable="d" d:ExtensionType="Search">
    <Page.Resources>
        <Style x:Key="GridViewItemStyleSmall" TargetType="GridViewItem">
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>
        <DataTemplate x:Key="ControlItemTemplate" x:DataType="data:ControlInfoDataItem">
            <Grid x:Name="controlRoot" Height="145" Width="345" Padding="15" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image x:Name="gridImage" Source="{x:Bind ImagePath}" Stretch="Uniform" Width="36" VerticalAlignment="Top" AutomationProperties.Name="{x:Bind Title}" />
                <RelativePanel Grid.Column="1" Margin="32,0,0,0">
                    <TextBlock Text="{x:Bind Title}" Style="{StaticResource SubtitleTextBlockStyle}" x:Name="titleText" TextLineBounds="TrimToCapHeight" TextWrapping="NoWrap" FontWeight="SemiBold" Foreground="{ThemeResource HyperlinkButtonForeground}" />
                    <TextBlock Text="{x:Bind Subtitle}" Style="{StaticResource BodyTextBlockStyle}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" RelativePanel.Below="titleText" />
                </RelativePanel>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ControlItemTemplateSmall" x:DataType="data:ControlInfoDataItem">
            <Grid x:Name="controlRoot" Height="145" Width="332" Padding="15" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image x:Name="gridImage" Source="{x:Bind ImagePath}" Stretch="Uniform" Width="36" VerticalAlignment="Top" AutomationProperties.Name="{x:Bind Title}" />
                <RelativePanel Grid.Column="1" Margin="32,0,0,0">
                    <TextBlock x:Name="titleText" Text="{x:Bind Title}" Style="{StaticResource SubtitleTextBlockStyle}" TextLineBounds="TrimToCapHeight" TextWrapping="NoWrap" FontWeight="SemiBold" Foreground="{ThemeResource HyperlinkButtonForeground}" />
                    <TextBlock Text="{x:Bind Subtitle}" Style="{StaticResource BodyTextBlockStyle}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" RelativePanel.Below="titleText" />
                </RelativePanel>
            </Grid>
        </DataTemplate>
        <DataTemplate x:DataType="local:Filter" x:Key="filterTemplate">
            <GridView x:Name="resultsGridView" ItemsSource="{x:Bind Items}" AutomationProperties.AutomationId="ResultsGridView" Margin="0,24,0,0" AutomationProperties.Name="Search Results" TabIndex="1" SelectionMode="None" IsSwipeEnabled="false" IsItemClickEnabled="True" ItemClick="resultsGridView_ItemClick" ItemTemplate="{StaticResource ControlItemTemplate}" ItemContainerStyle="{StaticResource GridViewItemStyle}" />
        </DataTemplate>
        <DataTemplate x:DataType="local:Filter" x:Key="filterTemplateSmall">
            <GridView x:Name="resultsGridView" ItemsSource="{x:Bind Items}" AutomationProperties.AutomationId="ResultsGridView" Margin="0,14,0,0" AutomationProperties.Name="Search Results" TabIndex="1" SelectionMode="None" IsSwipeEnabled="false" IsItemClickEnabled="True" ItemClick="resultsGridView_ItemClick" ItemTemplate="{StaticResource ControlItemTemplate}" ItemContainerStyle="{StaticResource GridViewItemStyleSmall}" />
        </DataTemplate>
    </Page.Resources>
    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <RelativePanel Background="{StaticResource HomePageBackgroundBrush}">
        <RelativePanel.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition />
            </TransitionCollection>
        </RelativePanel.ChildrenTransitions>
        <Pivot x:Name="resultsPivot" ItemsSource="{x:Bind Filters, Mode=OneWay}" Margin="12,0,24,0" ItemTemplate="{StaticResource filterTemplate}" Loaded="resultsPivot_Loaded" KeyDown="resultsPivot_KeyDown">
            <Pivot.HeaderTemplate>
                <DataTemplate x:DataType="local:Filter">
                    <TextBlock Text="{x:Bind Description}" />
                </DataTemplate>
            </Pivot.HeaderTemplate>
        </Pivot>
        <TextBlock x:Name="noResultsTextBlock" Grid.Row="1" Visibility="Collapsed" Margin="24,24,0,0" Style="{StaticResource TitleTextBlockStyle}" Text="No results match your search." />
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="ResultStates">
                <VisualState x:Name="ResultsFound" />
                <!-- When there are no results, the results panel is replaced with an informational TextBlock -->
                <VisualState x:Name="NoResultsFound">
                    <VisualState.Setters>
                        <Setter Target="resultsPivot.Visibility" Value="Collapsed" />
                        <Setter Target="noResultsTextBlock.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters></VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="resultsPivot.Margin" Value="0" />
                        <Setter Target="noResultsTextBlock.Margin" Value="14,14,0,0" />
                        <Setter Target="resultsPivot.ItemTemplate" Value="{StaticResource filterTemplateSmall}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </RelativePanel>
</Page>
